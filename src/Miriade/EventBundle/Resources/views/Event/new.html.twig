{% extends 'EventEventBundle::layout.html.twig' %}

{% block content %}
    <div id="wrap">
        <div class="titre">
            <h2>Création d'une événement</h2>
            <hr>
        </div>
        {{ form_start(form, {"action": path('event_event_event_create')} ) }}
        <p>{{ form_label(form.title) }}</p>
        <p>{{ form_widget(form.title) }}</p>

        {{ form_end(form) }}
    </div>
{% endblock %}
