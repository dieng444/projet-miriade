{% extends '::base.html.twig' %}

{% block header %}
    <header class='boxShadow1'>
        {#{%if is_granted("ROLE_USER")%}#}
            {#{%if owner_fiche_exist==false%}#}
            <div class="head-button">
                <a href="{{ path('event_event_event_dashboard') }}" class='btn boxShadow1' type='button'>La Carte</a>
                <a href="{{ path('carto_fiche_fiche_new') }}" class="btn boxShadow1">Votre Initiative</a>
                {%if is_granted("ROLE_SUPER_ADMIN")%}
                    <a href="{{ path('carto_admin_manageFiches') }}" class="btn boxShadow1">Administration</a>
                {%endif%}
                <a href="{{ path('fos_user_security_logout') }}" class="btn boxShadow1">Déconnexion</a>
            </div>
        {#{% else %}#}
            {#<div class="head-button">#}
                {#<a href="{{ path('fos_user_registration_register') }}" class='btn boxShadow1'>S'inscrire</a>#}
                {#<a href="{{ path('fos_user_security_login') }}" class="btn boxShadow1">Se connecter</a>#}
            {#</div>#}
        {#{%endif%}#}


    </header>
{% endblock %}

{% block footer %}
    <footer>
        <p>© BDIS 2015 tous droits réservés</p>
        <p><a href="#">Contacts</a> | <a href="{{ path('carto_fiche_dashboard_credits') }}">Crédits</a> | <a href="{{ path('carto_fiche_dashboard_mentions') }}">Mentions légales</a> | <a href="{{ path('carto_fiche_dashboard_charte') }}">Charte BDIS</a> | <a href="http://la-bdis.org/">Site national</a></p>
    </footer>
{% endblock %}
